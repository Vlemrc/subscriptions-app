import Navbar from "../components/Navbar";

const Account = () => {
    return (
        <div>
            <Navbar activeItem='account' />
        <h1>Account</h1>
        </div>
    );
};

export default Account;